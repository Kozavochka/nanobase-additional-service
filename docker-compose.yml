version: "3.9"
services:
  fastapi-app:
    build:
      context: .
    ports:
      - "5000:5000"
    environment:
      - ENV_FILE=.env
    env_file:
      - .env  
    networks:
      - nano


networks:
  nano:
    driver: bridge
    external: true
